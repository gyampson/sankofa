gsap.from(".h1", { duration: 3, x: -300, ease: "power2.inOut" });
gsap.from("h2", { duration: 1, y: -100, ease: "power2.inOut" });
gsap.from("header", { duration: 3, x: -100, ease: "power2.inOut" });
gsap.from("section", { duration: 2, y: -100, ease: "power2.inOut" });
gsap.from(".logo1", { duration: 1, y: -100, ease: "power2.inOut" });
gsap.from(".main-banner", { duration: 3, y: -200, ease: "power2.inOut" });
gsap.from(".pic", { duration: 3, x: -500, ease: "power2.inOut" });
gsap.from(".get-button", { duration: 3, y: 500, ease: "power2.inOut" });
gsap.from(".box", { duration: 1, y: -100, ease: "power2.inOut" });
gsap.from("footer", { duration: 2, y: -100, ease: "power2.inOut" });
